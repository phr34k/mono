# systemd-boot global config (lands on the ESP as /loader/loader.conf).
# Boot entries themselves are the BLS Type#1 files ostree writes to the boot
# partition (/boot/loader/entries/*.conf); systemd-boot auto-selects the newest.
timeout 3
console-mode auto
